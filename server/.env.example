# Render / Supabase – copy to .env locally; set these in Render Dashboard for production

# Supabase: Project Settings → Database → Connection string (URI). Use "Connection pooling" for serverless.
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Or individual vars (optional):
# DB_USER=
# DB_PASSWORD=
# DB_HOST=
# DB_PORT=5432
# DB_NAME=postgres

PORT=5000

# Vercel frontend URL (for CORS and Stripe redirects). No trailing slash.
CLIENT_URL=https://your-app.vercel.app

# JWT secret (generate a long random string)
JWT_SECRET=your-super-secret-jwt-key

# Stripe: Dashboard → Developers → API keys
STRIPE_SECRET_KEY=sk_live_...

# Google AI (Gemini): https://aistudio.google.com/app/apikey
GEMINI_API_KEY=...
