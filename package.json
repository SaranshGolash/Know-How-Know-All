{
  "name": "knowhowknowall-root",
  "version": "1.0.0",
  "scripts": {
    "start": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "test": "cd client && npm test"
  },
  "engines": {
    "node": "24.x"
  }
}
